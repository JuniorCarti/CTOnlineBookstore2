<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:orientation="vertical"
    android:background="@android:color/white"
    tools:ignore="ExtraText">

    <!-- Back Button -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_back"/>  <!-- Make sure you have an icon called ic_back -"/>

    <!- Reset Animation -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieResetAnimation"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:lottie_fileName="password_reset.json"
        app:lottie_autoPlay="true"
        app:lottie_loop="false"
        android:layout_gravity="center"/>

    <!-- Header: Reset Password -->
    <TextView
        android:id="@+id/tvResetPasswordHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="Reset Password?"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:gravity="center"
        android:layout_gravity="center"/>

    <!-- Email Input Field -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/emailInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomEnd="12dp"
        android:hint="Enter your email">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etResetEmailAddress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:contentDescription="Email input field for reset password"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Instruction Text: Email Check -->
    <TextView
        android:id="@+id/tvEmailCheckInstructions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="*Check your email to receive reset password instructions"
        android:textSize="14sp"
        android:textColor="@android:color/holo_red_light"
        android:textStyle="italic"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:layout_gravity="center"/>

    <!-- ProgressBar for loading -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:visibility="gone"
        android:layout_gravity="center"/>

    <!-- Submit Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSubmitReset"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Submit"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        app:backgroundTint="@color/button_yellow"
        app:cornerRadius="12dp"
        android:contentDescription="Submit button for resetting password"/>

</LinearLayout>
